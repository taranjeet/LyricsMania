# -*- mode: python -*-
a = Analysis(['windows_lyrics.py'],
             pathex=['C:\\Python27\\PyInstaller-2.1\\PyInstaller-2.1\\windows_lyrics'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='windows_lyrics.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='windows_lyrics')
